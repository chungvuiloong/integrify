import React from 'react';
import ProfileCard from './ProfileCard';

export default function Homepage () {
        return (
          <div>
            <ProfileCard/>
          </div>
        )
  }
